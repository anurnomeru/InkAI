# 国际化（i18n）

- 实现：`ui/i18n.py` 提供 `t(s)`，从 `ui/locales/zh_CN.json` 读取词条；未命中时回退原文。
- 本地化文件：`ui/locales/zh_CN.json`。
- 兼容性：为避免某些系统字体导致的乱码，个别 UI 文案使用英文常量（如 Draft Variants）。
- 指南：新增文案优先调用 `t()`，并在 `zh_CN.json` 添加入词；其它语言可追加对应的 json。
